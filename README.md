

# 🌌 PixelEncoder v6.2 (Post-Quantum Edition)

**PixelEncoder** — это утилита для надежного сокрытия файлов и текста внутри PNG-изображений с использованием современных постквантовых алгоритмов шифрования. 

Программа использует асимметричную криптографию **ML-KEM-768 (Kyber)** для защиты ключей и **AES-256-GCM** для шифрования самих данных. Никто не сможет извлечь ваши данные из картинки без правильного приватного ключа.

## 🚀 Быстрый старт (Интерактивный режим)

Самый простой способ использовать программу — просто **запустить `pixel_encoder_pq.exe` двойным кликом**. 

Откроется удобное интерактивное меню:
1. Выберите нужный пункт меню цифрами (1, 2, 3 и т.д.).
2. Программа сама пошагово спросит вас, что делать.
3. Поддерживается перетаскивание файлов (Drag & Drop) прямо в окно консоли, а также любые пути (относительные `./file`, абсолютные `C:\`, переменные среды `%USERPROFILE%\...`).

### Основной сценарий работы:

#### Шаг 1: Создание ключей (KeyGen)
Выберите пункт `[3] KeyGen`. Программа создаст два файла:
* 🔓 `public.kyber` — **Публичный ключ**. Отдайте его тому, кто будет шифровать для вас файлы (или используйте сами для шифрования).
* 🔐 `private.kyber` — **Приватный ключ**. Храните его в строжайшем секрете! Он нужен **только вам** для расшифровки.

#### Шаг 2: Шифрование (Encode)
Выберите `[1] Encode`.
1. Укажите, что хотите спрятать: файл (до 100 МБ) или просто введите текст.
2. Укажите путь к публичному ключу получателя (`public.kyber`).
3. Укажите, как назвать итоговую картинку (например, `secret.png`).
*Готово! Теперь ваши данные надежно зашифрованы и спрятаны в PNG-изображении.*

#### Шаг 3: Расшифровка (Decode)
Выберите `[2] Decode`.
1. Укажите путь к картинке с секретом (`secret.png`).
2. Укажите путь к **вашему** приватному ключу (`private.kyber`).
3. Выберите папку для сохранения извлеченного файла.
*Программа проверит целостность, расшифрует данные и восстановит оригинальный файл.*

---

## 💻 Использование через командную строку (CLI)

Для продвинутых пользователей и автоматизации поддерживается запуск из консоли (CMD/PowerShell).

### Генерация ключей:
```cmd
pixel_encoder_pq.exe keygen ./keys_folder
```

### Шифрование (Прячем файл в картинку):
```cmd
pixel_encoder_pq.exe encode путь\к\public.kyber -f секретный_файл.zip -o результат.png
```
*Для шифрования текста из консоли используйте флаг `-t "Ваш текст"` вместо `-f`.*

### Расшифровка (Достаем файл из картинки):
```cmd
pixel_encoder_pq.exe decode результат.png путь\к\private.kyber -d ./папка_для_сохранения
```

---

## 📌 Важные примечания

* **Ограничение размера:** Максимальный размер скрываемого файла — **100 МБ**.
* **Размер картинки:** Итоговый размер PNG напрямую зависит от размера исходного файла (чем больше файл, тем больше разрешение картинки).
* **Безопасность ключей:** Если вы потеряете `private.kyber`, восстановить данные из картинки будет **невозможно**.
* **Целостность данных:** Картинки нельзя сжимать с потерями (например, переводить в JPEG или отправлять через мессенджеры, которые "шакалят" качество, как Telegram без галочки "Отправить как файл"). Иначе данные повредятся.

## 🛠 Устранение неполадок
Если при запуске возникает ошибка, связанная с `oqs.dll` или `liboqs`:
Убедитесь, что все необходимые библиотеки (DLL), если они шли в архиве с `.exe`, лежат в одной папке с программой. Также можно вручную указать путь к DLL через системную переменную окружения `LIBOQS_DLL_DIR`.
